schema: ./schema.graphql
documents: src/**/*.ts
generates:
  src/generated/graphql.ts:
    plugins:
      - typescript
      - typescript-operations
      - typescript-document-nodes
      - typed-document-node
